---
title: 'API Routes'
---

## Creating Endpoints

Next.js allows you to easily create API endpoints by adding files to the `pages/api` directory.

Each file in this directory is mapped to an `/api/*` route and is treated as a server-side bundle, not a client-side page.

This is perfect for handling form submissions, database queries, or authentication logic without needing a separate backend server.
