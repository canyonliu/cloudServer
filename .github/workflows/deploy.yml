name: Build and Deploy Artifacts (构建并部署产物)

on:
  push:
    branches: ["deploy"]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository (检出代码)
        uses: actions/checkout@v4

      - name: Setup Node.js (设置Node.js环境)
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Dependencies (安装依赖)
        run: npm ci

      - name: Build Next.js Application (构建Next.js应用)
        run: npm run build

      - name: Create Deployment Package (创建部署压缩包)
        run: |
          mkdir -p public deployment-package
          cp -r .next deployment-package/
          if [ -d "public" ]; then
            cp -r public deployment-package/
          fi
          cp next.config.js package.json Dockerfile deployment-package/
          tar -czf deployment-package.tar.gz -C deployment-package .

      - name: Deploy to Server via SCP (通过SCP部署到服务器)
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "deployment-package.tar.gz"
          target: "~/deploy/clound_云服务/cloudServer"

      - name: Unpack and Run on Server (在服务器上解压并运行)
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd ~/deploy/clound_云服务/cloudServer
            tar -xzf deployment-package.tar.gz
            sudo docker build -t my-next-app:${{ github.sha }} .
            if [ $(sudo docker ps -q -f name=my-running-cloud) ]; then 
              sudo docker stop my-running-cloud && 
              sudo docker rm my-running-cloud; 
            fi
            sudo docker run -d -p 80:3000 --name my-running-cloud --env NEWS_API_KEY=${{ secrets.NEWS_API_KEY }} my-next-app:${{ github.sha }}
            sudo docker image prune -f